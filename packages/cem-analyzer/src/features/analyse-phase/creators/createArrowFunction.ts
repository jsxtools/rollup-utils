import type { CEM, TS } from "../../../types.js";

/** Builds a function declaration from an arrow variable. */
export declare function createArrowFunction(node: TS.VariableStatement): CEM.FunctionDeclaration;
